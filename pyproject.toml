[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sql-query-optimizer"
version = "0.1.0"
description = "SQL query analysis and optimization suggestions"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
sql-optimize = "optimizer.cli:main"
